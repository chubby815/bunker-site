=== PRICING SECTION at 35440 ===
id="pricing" class="section">
  <div class="container">
    <div style="text-align:center; max-width:560px; margin:0 auto 60px;">
      <span class="label">Pricing</span>
      <h2>Simple. One-time. No tricks.</h2>
      <p class="sub" style="margin:0 auto;">Two tiers. Both run locally. No data leaves your machine.</p>
    </div>
    <div class="compare">
      <div class="compare-col">
        <div class="compare-header">
          <div class="plan">Free — BYOK</div>
          <div class="price">$0 <span>/ forever</span></div>
          <div class="desc">Bring your own API key. You handle the agent setup.</div>
        </div>
        <div class="compare-body">
          <div class="compare-item"><span class="check">✓</span> <span>Bunker editor — full access</span></div>
          <div class="compare-item"><span class="check">✓</span> <span>Local API for your agent</span></div>
          <div class="compare-item"><span class="check">✓</span> <span>Any model via your own key</span></div>
          <div class="compare-item"><span class="check">✓</span> <span>File read/write/run commands</span></div>
          <div class="compare-item"><span class="cross">✕</span> <span style="color:var(--muted)">Pre-built BYOA agent (Bailey)</span></div>
          <div class="compare-item"><span class="cross">✕</span> <span style="color:var(--muted)">Telegram command integration</span></div>
          <div class="compare-item"><span class="cross">✕</span> <span style="color:var(--muted)">Priority community votes</span></div>
          <div style="margin-top:24px;">
            <a href="#capture" class="btn btn-outline" style="width:100%; justify-content:center;">Get free access</a>
          </div>
        </div>
      </div>
      <div class="compare-col featured">
        <div class="compare-header">
          <div class="plan">BYOA — One-time</div>
          <div class="price">$99 <span>/ once</span></div>
          <div class="desc">Includes Bailey, the pre-built agent. Plug in your API key and go.</div>
        </div>
        <div class="compare-body">
          <div class="compare-item"><span class="check-blue">✓</span> <span>Everything in Free</span></div>
          <div class="compare-item"><span class="check-blue">✓</span> <span>Bailey — pre-built BYOA agent</span></div>
          <div class="compare-item"><span class="check-blue">✓</span> <span>Telegram command interface</span></div>
          <div class="compare-item"><span class="check-blue">✓</span> <span>Screenshot + browser console reads</span></div>
          <div class="compare-item"><span class="check-blue">✓</span> <span>Persistent memory across sessions</span></div>
          <div class="compare-item"><span class="check-blue">✓</span> <span>Priority feature votes</span></div>
          <div class="compare-item"><span class="check-blue">✓</span> <span>All future updates — lifetime</span></div>
          <div style="margin-top:24px;">
            <a href="#capture" class="btn btn-primary" style="width:100%; justify-content:center;">Get Bunker — $99</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── EMAIL CAPTURE ──────────────────────────────────────────────── -->
<section id="email">
  <div class="container">
    <span class="label" style="display:block;text-align:center;">Early access</span>
    <h2>Be first in.</h2>
    <p class="sub">Drop your email. We'll tell you when the doors open — no spam, one email.</p>
    <form class="email-form" onsubmit="handleSubmit(event)">
      <input type="email" id="email-input" placeholder="you@example.com" required autocomplete="email" />
      <button type="submit" class="btn btn-primary">Notify me</button>
    </form>
    <p id="form-msg" class="mono" style="margin-top:20px;font-size:13px;color:var(--blue-hi);min-height:20px;text-align:center;"></p>
  </div>
</section>

<!-- ─── FOOTER ─────────────────────────────────────────────────────── -->
<footer>
  <div class="footer-inner">
    <a href="#" 

=== MODELS TEXT at 26626 ===
r machine.</h2>
        <p class="sub">
          Claude, GPT-4o, Gemini, Grok — or run Llama 3 free with Ollama locally.
          You bring the API key. Bunker doesn't touch it. You're not locked to
          anyone's model roadmap.
        </p>
        <div class="model-grid

=== ALL MODEL PILLS ===
model-pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 9px 16px;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    background: var(--bg2);
    font-family: var(--mono);
    font-size: 13px;
    color: var(--white);
    transition: border-color 0.2s, box-shadow 0.2s;
    cursor: default;
  }
  .model-pill:hover {
    border-color: var(--blue);
    box-shadow: var(--glow);
  }

  .model-pill .dot-m {
    width: 7px; height: 7px;
    border-radius: 50%;
  }
  .green-dot  { background: #28c840; box-shadow: 0 0 5px #28c840; }
  .orange-dot { background: #ff9500; box-shadow: 0 0 5px #ff9500; }
  .blue-dot   { background: var(--blue-hi); box-shadow: 0 0 5px var(--blue-hi); }
  .pink-dot   { background: #ff2d55; box-shadow: 0 0 5px #ff2d55; }
  .purple-dot { background: #bf5af2; box-shadow: 0 0 5px #bf5af2; }

  /* ─── COMPARE TABLE ────────────────────────────────────────────── */
  .compare {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-top: 52px;
  }

  .compare-col {
    border: 1px solid var(--border);
    border-radius: 10px;
    overflow: hidden;
  }

  .compare-col.featured {
    border-color: var(--blue);
    box-shadow: var(--glow);
  }

  .compare-header {
    padding: 24px 28px;
    border-bottom: 1px solid var(--border);
    background: var(--bg2);
  }

  .compare-header .plan {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 8px;
  }

  .compare-header .price {
    font-size: 36px;
    font-weight: 700;
    letter-spacing: -0.04em;
    color: var(--white);
    line-height: 1;
  }

  .compare-header .price span {
    font-size: 14px;
    font-weight: 400;
    color: var(--muted);
    letter-spacing: 0;
  }

  .compare-header .desc {
    font-size: 13px;
    color: var(--muted);
    margin-top: 8px;
  }

  .compare-col.featured .compare-header { background: rgba(30,144,255,0.06); }

  .compare-body {
    padding: 24px 28px;
    background: var(--bg);
  }

  .compare-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 10px 0;
    border-bottom: 1px solid var(--border);
    font-size: 14px;
    color: var(--muted);
  }
  .compare-item:last-child { border-bottom: none; }

  .check {
    color: #28c840;
    font-size: 15px;
    flex-shrink: 0;
    margin-top: 1px;
  }
  .cross { color: #ff3b30; }
  .check-blue { color: var(--blue-hi); }

  .compare-item span { color: var(--white); }

  /* ─── STATS ────────────────────────────────────────────────────── */
  .stats-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: 10px;
    overflow: hidden;
    margin-top: 64px;
  }

  .stat {
    background: var(--bg);
    padding: 40px 36px;
    text-align: center;
  }

  .stat .num {
    font-size: 44px;
    font-weight: 700;
    letter-spacing: -0.04em;
    color: var(--blue-hi);
    text-shadow: 0 0 20px rgba(0,191,255,0.3);
    display: block;
    line-height: 1;
    margin-bottom: 8px;
    font-family: var(--mono);
  }

  .stat .lbl {
    font-size: 14px;
    color: var(--muted);
  }

  /* ─── TIMELINE / STEPS ─────────────────────────────────────────── */
  .steps {
    display: flex;
    flex-direction: column;
    gap: 0;
    margin-top: 52px;
    position: relative;
  }

  .steps::before {
    content: '';
    position: absolute;
    left: 19px;
    top: 8px;
    bottom: 8px;
    width: 1px;
    background: linear-gradient(to bottom, var(--blue), transparent);
  }

  .step {
    display: flex;
    gap: 24px;
    padding: 20px 0;
    position: relative;
  }

  .step-num {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid var(--blue);
    background: var(--bg);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--mono);
    font-size: 13px;
    color: var(--blue-hi);
    flex-shrink: 0;
    box-shadow: 0 0 12px rgba(30,144,255,0.2);
    position: relative;
    z-index: 1;
  }

  .step-body h3 {
    font-size: 16px;
    font-weight: 600;
    color: var(--white);
    margin-bottom: 6px;
    margin-top: 8px;
  }

  .step-body p {
    font-size: 14px;
    color: var(--muted);
    line-height: 1.6;
  }

  /* ─── EMAIL CAPTURE ────────────────────────────────────────────── */
  #capture {
    text-align: center;
    padding: 100px 0;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    background: radial-gradient(ellipse at center, rgba(30,144,255,0.05) 0%, transparent 65%);
  }

  #capture h2 { max-width: 500px; margin: 12px auto 16px; }
  #capture .sub { margin: 0 auto 40px; text-align: center; }

  .email-form {
    display: flex;
    gap: 10px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .email-form input {
    padding: 13px 18px;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    background: var(--bg2);
    color: var(--white);
    font-size: 15px;
    font-family: var(--sans);
    width: 300px;
    outline: none;
    transition: border-color 0.18s, box-shadow 0.18s;
  }
  .email-form input::placeholder { color: var(--muted); }
  .email-form input:focus {
    border-color: var(--blue);
    box-shadow: var(--glow);
  }

  /* ─── FOOTER ───────────────────────────────────────────────────── */
  footer {
    padding: 48px 0;
    background: var(--bg);
  }

  .footer-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 32px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 20px;
  }

  .footer-logo {
    display: flex;
    align-items: center;
    gap: 8px;
    text-decoration: none;
  }

  .footer-logo img {
    width: 40px;
    height: 40px;
    object-fit: contain;
    filter: drop-shadow(0 0 5px rgba(30,144,255,0.6));
  }

  .footer-logo span {
    font-family: var(--mono);
    font-size: 14px;
    color: var(--muted);
  }

  .footer-copy {
    font-size: 13px;
    color: var(--muted);
    font-family: var(--mono);
  }

  .footer-links {
    display: flex;
    gap: 24px;
    list-style: none;
  }

  .footer-links a {
    font-size: 13px;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.15s;
  }
  .footer-links a:hover { color: var(--white); }

  /* ─── INLINE HIGHLIGHT BOX ─────────────────────────────────────── */
  .highlight-box {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 12px 20px;
    border-radius: var(--radius);
    border: 1px solid var(--blue-dim);
    background: rgba(30,144,255,0.07);
    font-family: var(--mono);
    font-size: 13px;
    color: var(--blue-hi);
    margin-top: 28px;
  }

  /* ─── OWN IT / CONTRAST ────────────────────────────────────────── */
  .contrast-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-top: 52px;
  }

  .contrast-box {
    padding: 32px;
    border-radius: 10px;
    border: 1px solid var(--border);
  }

  .contrast-box.bad {
    background: rgba(255,59,48,0.04);
    border-color: rgba(255,59,48,0.15);
  }

  .contrast-box.good {
    background: rgba(30,144,255,0.06);
    border-color: var(--blue-dim);
    box-shadow: var(--glow);
  }

  .contrast-box h3 {
    font-size: 15px;
    font-weight: 600;
    margin-bottom: 16px;
    font-family: var(--mono);
  }

  .contrast-box.bad h3 { color: #ff3b30; }
  .contrast-box.good h3 { color: var(--blue-hi); }

  .contrast-box ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .contrast-box li {
    font-size: 14px;
    color: var(--muted);
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .contrast-box.good li { color: var(--white); }

  /* ─── COMMUNITY SECTION ────────────────────────────────────────── */
  .vote-items {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 40px;
  }

  .vote-item {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 14px 20px;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    background: var(--bg2);
    transition: border-color 0.2s;
  }
  .vote-item:hover { border-color: var(--blue); }

  .vote-bar-wrap {
    flex: 1;
    height: 4px;
    background: var(--border);
    border-radius: 2px;
    overflow: hidden;
  }

  .vote-bar {
    height: 100%;
    background: linear-gradient(90deg, var(--blue), var(--blue-hi));
    border-radius: 2px;
    box-shadow: 0 0 6px var(--blue);
  }

  .vote-label {
    font-size: 14px;
    color: var(--white);
    min-width: 220px;
  }

  .vote-pct {
    font-family: var(--mono);
    font-size: 12px;
    color: var(--blue-hi);
    min-width: 36px;
    text-align: right;
  }

  /* ─── RESPONSIVE ───────────────────────────────────────────────── */
  @media (max-width: 800px) {
    .hero-inner, .split, .compare, .contrast-row, .stats-row { grid-template-columns: 1fr; }
    .split.reverse { direction: ltr; }
    .nav-links { display: none; }
    h1 { font-size: 36px; }
    .section { padding: 80px 0; }
    #hero { padding: 100px 0 80px; }
  }

  /* ─── LOGO IMG OVERRIDES ───────────────────────────────────────── */

  /* ─── LOGO IMAGES (transparent PNG — no blend needed) ─── */
  
  .hero-logo {
    height: 160px;
    width: auto;
    display: block;
    margin: 0 auto 24px;
    filter: drop-shadow(0 0 18px rgba(0,191,255,0.5));
  }
  .footer-logo img {
    height: 40px;
    width: auto;
    display: block;
  }

</style>
</head>
<body>

<!-- ─── NAV ──────────────────────────────────────────────────────── -->
<nav>
  <div class="nav-inner">
    <a href="#" class="nav-logo">
      <img src="logo.png" alt="Bunker logo" />
      <span>Bunker</span>
    </a>
    <ul class="nav-links">
      <li><a href="#byoa">How it works</a></li>
      <li><a href="#models">Models</a></li>
      <li><a href="#gamedev">Game dev</a></li>
      <li><a href="#pricing">Pricing</a></li>
      <li><a href="#community">Community</a></li>
    </ul>
    <div class="nav-right">
      <a href="#pricing" class="btn btn-ghost">See pricing</a>
      <a href="#pricing" class="btn btn-primary">Get Bunker&nbsp;&nbsp;$99</a>
    </div>
model-pill"><span class="dot-m orange-dot"></span>Claude 3.5 Sonnet</div>
model-pill"><span class="dot-m green-dot"></span>GPT-4o</div>
model-pill"><span class="dot-m blue-dot"></span>Gemini 1.5 Pro</div>
model-pill"><span class="dot-m pink-dot"></span>Grok 2</div>
model-pill"><span class="dot-m purple-dot"></span>Llama 3 (Ollama)</div>
model-pill"><span class="dot-m green-dot"></span>Mistral</div>
model-pill"><span class="dot-m blue-dot"></span>DeepSeek R1</div>
model-pill" style="border-style:dashed; color:var(--muted);">+ any OpenAI-compat</div>

=== VOTE PCT at 18599 ===
-bar-wrap {
    flex: 1;
    height: 4px;
    background: var(--border);
    border-radius: 2px;
    overflow: hidden;
  }

  .vote-bar {
    height: 100%;
    background: linear-gradient(90deg, var(--blue), var(--blue-hi));
    border-radius: 2px;
    box-shadow: 0 0 6px var(--blue);
  }

  .vote-label

=== VOTE PCT at 34204 ===
tem">
            <span class="vote-label">Agent screenshot feedback</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:91%"></div></div>
            <span class="vote-pct">91%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">M

=== VOTE PCT at 34257 ===
enshot feedback</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:91%"></div></div>
            <span class="vote-pct">91%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">Multi-agent support</span>
            <div class="vot

=== VOTE PCT at 34457 ===
vote-item">
            <span class="vote-label">Multi-agent support</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:78%"></div></div>
            <span class="vote-pct">78%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">G

=== VOTE PCT at 34510 ===
i-agent support</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:78%"></div></div>
            <span class="vote-pct">78%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">Git diff viewer</span>
            <div class="vote-ba

=== VOTE PCT at 34706 ===
ss="vote-item">
            <span class="vote-label">Git diff viewer</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:65%"></div></div>
            <span class="vote-pct">65%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">V

=== VOTE PCT at 34759 ===
Git diff viewer</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:65%"></div></div>
            <span class="vote-pct">65%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">Voice command mode</span>
            <div class="vote

=== VOTE PCT at 34958 ===
"vote-item">
            <span class="vote-label">Voice command mode</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:54%"></div></div>
            <span class="vote-pct">54%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">B

=== VOTE PCT at 35011 ===
ce command mode</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:54%"></div></div>
            <span class="vote-pct">54%</span>
          </div>
          <div class="vote-item">
            <span class="vote-label">Built-in Ollama manager</span>
            <div class=

=== VOTE PCT at 35215 ===
-item">
            <span class="vote-label">Built-in Ollama manager</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:48%"></div></div>
            <span class="vote-pct">48%</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── PRI

=== VOTE PCT at 35268 ===
 Ollama manager</span>
            <div class="vote-bar-wrap"><div class="vote-bar" style="width:48%"></div></div>
            <span class="vote-pct">48%</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ─── PRICING ────────────────────────────────────────────────

=== VOTE PCT at 37081 ===
)">Priority community votes</span></div>
          <div style="margin-top:24px;">
            <a href="#capture" class="btn btn-outline" style="width:100%; justify-content:center;">Get free access</a>
          </div>
        </div>
      </div>
      <div class="compare-col featured">
        <div clas